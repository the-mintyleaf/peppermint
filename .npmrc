# Hoist Mantine packages so TypeScript can resolve re-exports from @peppermint/ui
# (pnpm's isolated node_modules otherwise hides them from the IDE)
public-hoist-pattern[]=@mantine/*
public-hoist-pattern[]=@tiptap/*
auto-install-peers=true
store-dir=~/.pnpm-store
