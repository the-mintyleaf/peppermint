import type { TaskBoardFilter } from "./module.api";

export type { TaskBoardFilter };

// No props today; typed as an empty object so the component signature stays stable.
export type KanbanDashboardProps = Record<string, never>;
