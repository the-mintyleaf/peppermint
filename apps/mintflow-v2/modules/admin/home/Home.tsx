"use client";

import { Center, Title } from "@peppermint/ui";

export function ModuleHome() {
  return (
    <Center h="calc(100vh - 16px)" px="md">
      the kanban that tastes a little too fresh
    </Center>
  );
}
