"use client";

import { Center, Text } from "@peppermint/ui";

import type { CreateTaskProps } from "./CreateTask.types";

// Phase 0 stub — replaced by the create-task module build.
export function ModuleCreateTask(_props: CreateTaskProps) {
  return (
    <Center h="100%" p="xl">
      <Text c="dimmed">Create Task</Text>
    </Center>
  );
}
