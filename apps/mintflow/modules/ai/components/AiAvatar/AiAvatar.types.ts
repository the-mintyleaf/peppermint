export interface AiAvatarProps {
  /** Square side length in px. */
  size?: number;
  /** Corner radius in px. */
  radius?: number;
  /** Sparkle icon size in px (defaults to ~55% of `size`). */
  iconSize?: number;
}
