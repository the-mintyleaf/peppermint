import { createQueryKeys } from "@peppermint/admin";

/** Array-form keys for the admin signatures list (mirrors users/security-events). */
export const signaturesQueryKeys = createQueryKeys("documents.signatures");
