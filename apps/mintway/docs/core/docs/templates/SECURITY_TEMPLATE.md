<!-- Copy this file to <app>/docs/SECURITY.md and fill in. Delete this comment line.
     Only create this file when the app makes its own non-trivial security-relevant
     decisions beyond the project's standard auth pattern already documented in
     authenticate/docs/SECURITY.md (e.g. a custom token scheme, a novel grant/decision
     algorithm, custom rate-limiting). Don't create it just to have one. -->

# Security — <App Display Name>

**Owner app:** `<app_name>`
**Version:** <semver>
**Status:** Active | Draft | Deprecated
**Created:** YYYY-MM-DD

This document is required per project rulebook §19 (Documentation Requirements) because `<app_name>` makes its own security-relevant decisions beyond the project's standard auth pattern.

## Change History

| Version | Date       | Author      | Summary                        |
| ------- | ---------- | ----------- | ------------------------------ |
| 1.0.0   | YYYY-MM-DD | AI (Claude) | Initial security documentation |

---

## 1. <Topic, e.g. Token Handling / Rate Limiting / Decision Algorithm>

<prose / tables / code as needed>

## 2. <Next topic>

...
