# Gaps & assumptions

What the backend docs do **not** answer. Each entry is a question for the backend
or an assumption the frontend is making — **ask, don't invent**. Nothing here may
be silently resolved in code without confirmation.

| #   | Gap                                                                                                                                                                                                                                                                                                                                                                                                 | Impact on frontend                                                                                                                                          | Assumption (if any)                                                                                                                                                    | Where the assumption lives              |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| 1   | Exact **401 / 403 response bodies** are not shown (only status codes).                                                                                                                                                                                                                                                                                                                              | Can't type the auth-error body; rely on status code + the standard error envelope.                                                                          | Auth errors follow the standard `error` envelope with a `code`.                                                                                                        | api client 401/403 handling             |
| 2   | Full **serializer validation-error body** (`error.details` shape) is not enumerated per field.                                                                                                                                                                                                                                                                                                      | Can't reliably map field errors to form fields.                                                                                                             | Treat `error.details` as an **opaque object**; fall back to `error.message`. Do not hard-map a shape.                                                                  | form error mapper                       |
| 3   | **Default page size** (when `?page_size` is omitted) is not stated (only max 100).                                                                                                                                                                                                                                                                                                                  | Table page size / prefetch counts are a guess.                                                                                                              | Assume the DRF project default until confirmed; always send an explicit `page_size`.                                                                                   | list query params                       |
| 4   | **Default ordering** (when `?ordering` is omitted) is not stated.                                                                                                                                                                                                                                                                                                                                   | Row order is non-deterministic across environments.                                                                                                         | Always send an explicit `ordering` (from the whitelisted set); don't rely on a server default.                                                                         | list query params                       |
| 5   | Whether **staff may read every client** or only a scoped subset (there is no tenant/owner field yet).                                                                                                                                                                                                                                                                                               | Affects list scoping expectations for staff.                                                                                                                | Role-only access (no per-record ownership filter); all staff see all non-archived clients.                                                                             | client list query                       |
| 6   | **Domain-event outbox / global event ledger deferred** (concept §16.2). The admin-only **audit-trail read endpoint now exists** (`GET /clients/{id}/audit-events/`, paginated, newest first — see `client.md` §3), but there is **no** event broker / outbox streaming these events to an external consumer yet.                                                                                    | An audit "history / activity" panel _can_ now be built (admin-only). No cross-app event subscription / real-time feed is available.                         | Build the audit panel from `audit-events` (admin-only, paginated). Do **not** wire any event-stream / webhook consumer — none is shipped.                              | audit history panel                     |
| 7   | **Ranked search returns no per-row match reason** — the `matched_field` / `matched_value` per-row hint (concept §10.3, deferred "when practical") is **not** emitted; only the overall ranking is.                                                                                                                                                                                                  | Can't show _why_ a row matched (e.g. a "matched on alias" badge) in list search.                                                                            | Render ranked rows without a per-row match-reason badge; the boolean signal flags exist **only** on the duplicate-warning / duplicate-check payload, not on list rows. | list search results UI                  |
| 8   | **Merge** and **bulk CSV import/export** are **delivered (Phase 5)** — merge (`POST /clients/{id}/merge/`, admin+), merge-history (`GET /clients/{id}/merge-history/`, admin+), CSV export (`GET /clients/export/`, staff+), CSV import (`POST /clients/import/`, admin+). **Open:** import/export are **CSV-only** — **XLSX is a future extension** (needs a new backend dependency; not shipped). | Build the merge + CSV import/export UIs now (see `client.md` §3, `flows.md`). Do **not** build an XLSX upload/download path — only `text/csv` is supported. | CSV is the only bulk format this phase; treat XLSX as unavailable until a backend version lands it.                                                                    | admin client tools / bulk import-export |
| 9   | The **normalized\_\* search projections** (`normalized_email/phone/registration/tax`) are computed but **not serialized**.                                                                                                                                                                                                                                                                          | Cannot display or rely on them; only `website_domain` is exposed (admin list + detail).                                                                     | Treat them as internal search keys only; never expect them in a response.                                                                                              | `client.md` §1 / §7                     |
| 10  | **Pagination / default ordering of the child list endpoints** (contacts, addresses, aliases, tag assignments, tag catalog) is not explicitly documented.                                                                                                                                                                                                                                            | Child-list page size and row order are a guess.                                                                                                             | Assume the same `StandardPagination` (`page` / `page_size`) as the client list; send an explicit `page_size`; don't rely on a server default order.                    | child list query params                 |
| 11  | Whether **assigning a tag by a new free-typed `name` should be gated** (open catalog vs. admin-curated vocabulary) is not stated — any staff assign can create a catalog tag.                                                                                                                                                                                                                       | Tag catalog can grow unbounded / with near-duplicates staff typed differently.                                                                              | Treat the catalog as open (any staff assign resolves-or-creates); no separate "create tag" permission is enforced.                                                     | `tag.md` §4 / §7                        |

> **Phase status (concept §24):** all five phases are now **complete** — Phase 1
> client core, Phase 2 nested children + logo, Phase 3 ranked search + duplicate
> integrity, Phase 4 document-prefill + audit trail, and Phase 5 merge + bulk CSV.
> The **domain-event outbox / global event ledger stays deferred** (gap #6 — the
> Phase 4 decision holds): the admin-only audit-trail read exists, but there is no
> event broker / outbox streaming events to an external consumer, and none is
> planned this cycle.

> When a gap is resolved (backend answers, or a new pack version lands), delete its
> row and fold the truth into the relevant entity file — don't leave stale
> assumptions.
