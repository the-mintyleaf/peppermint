# Gaps & assumptions

What the backend docs do **not** answer. Each entry is a question for the backend
or an assumption the frontend is making — **ask, don't invent**. Nothing here may
be silently resolved in code without confirmation.

| #   | Gap                                                                                                                                                                                                                       | Impact on frontend                                                       | Assumption (if any)                                                                                                                 | Where the assumption lives                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| 1   | **No endpoint sets `profile_image`.** It is exposed read-only (URL / `null`) and a `AUTH_PROFILE_IMAGE_INVALID` code exists, but no upload/change route is wired in `urls.py`.                                            | Can't build a profile-image upload UI; can only display an existing URL. | Treat `profile_image` as display-only until an upload endpoint is confirmed; don't build the uploader.                              | `employee-profile.md` §1 / §7                  |
| 2   | **`email` / `phone` empty representation.** `API.md` §2.1's example shows `email: null` / `phone: null`, but the model fields (`contact_email`, `contact_phone`) are `blank=True`, not nullable → they serialize as `""`. | Wrong guard (`== null` vs `=== ""`) would misdetect "unset".             | Typed as `string` and rendered as `""` when unset (per the empty-string convention). Confirm with backend.                          | `employee-profile.md` §1, `current-user.md` §6 |
| 3   | Full **serializer validation-error body** (`error.details` shape) is enumerated only for the password case (`details.password: string[]`). Other field errors' shape is not documented.                                   | Can't reliably map every field error to a form field.                    | For password errors, read `details.password`; otherwise treat `error.details` as an opaque object and fall back to `error.message`. | form error mapper                              |
| 4   | Exact **401 / 403 response bodies** are not shown (only status + code).                                                                                                                                                   | Can't type the auth-error body precisely.                                | They follow the standard `error` envelope with a `code`; switch on the code.                                                        | api client 401/403 handling                    |
| 5   | **Default ordering** of the `/users/` and `/security-events/` lists (when no ordering param is given) is not stated; no `?ordering` param is documented for either.                                                       | Row order may be non-deterministic across environments.                  | Rely on the server's default order; there is no client `ordering` control until confirmed.                                          | list query params                              |
| 6   | The **`?search`** parameter on `/users/` — which fields it matches (username, name, employee_code?) — is not specified.                                                                                                   | Can't set search-field expectations in the UI copy.                      | Treat it as a broad free-text search; don't promise which fields it covers.                                                         | `user-account.md` §3                           |
| 7   | **`employee_profile` nullability** in the `UserAdmin` read: the serializer nests it directly, so a profileless account would yield `null`.                                                                                | A `null` here would break code that assumes it is always present.        | Every admin/staff account is created with a profile, so it is always present; typed non-null. Confirm.                              | `user-account.md` §2                           |
| 8   | **`SecurityEvent.metadata`** is arbitrary sanitized JSON — its per-event-type keys are not enumerated.                                                                                                                    | Can't type or render specific metadata fields generically.               | Typed `Record<string, unknown>`; render defensively / as key-value; don't hard-map keys.                                            | `security-event.md` §1                         |

> When a gap is resolved (backend answers, or a new doc version lands), delete its
> row and fold the truth into the relevant entity file — don't leave stale
> assumptions.
