Invoke the /code-structure skill on $ARGUMENTS (or on the most recently modified module if no argument provided).

Report findings: repeated logic, boundary violations, recommendation (extract / leave as-is / needs more context). Ask for confirmation before applying any refactor.
