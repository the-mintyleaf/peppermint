Invoke the /code-simplifier skill on $ARGUMENTS (or on files from `git diff --name-only HEAD` filtered to .ts/.tsx if no argument provided).

Report: files simplified, what changed, confirmation that functionality is unchanged.
